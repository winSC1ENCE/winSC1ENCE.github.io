[project]
name = "winsc1ence-site-tools"
version = "0.1.0"
description = "Utility tooling for site content (PDF parsing, etc.)"
authors = ["winSC1ENCE <unknown@example.com>"]
requires-python = ">=3.11"
dependencies = [
    "pypdf>=5.1.0",
]

[tool.uv]
package = false
